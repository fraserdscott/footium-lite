type Player @entity {
  id: ID!
  owner: Bytes!
  traits: [Int!]
}

type Match @entity {
  id: ID!
  accountA: Bytes!
  accountB: Bytes!
  requestId: Bytes!
  status: Int!
  formationA: [Int!]!
  formationB: [Int!]!
  seed: Int
  winner: Bytes
}
