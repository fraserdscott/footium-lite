type Owner @entity {
  id: ID!
  formation: [Int!]!
}

type Player @entity {
  id: ID!
  owner: Owner!
  traits: [Int!]
  image: String!
}

type Day @entity {
  id: ID!
  status: Int!
  requestId: Bytes
  randomNumber: Int
}
