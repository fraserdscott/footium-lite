type Player @entity {
  id: ID!
  owner: Bytes!
  traits: [Int!]
}

type Owner @entity {
  id: ID!
  formation: [Int!]!
}


type Match @entity {
  id: ID!
  accountA: Owner!
  accountB: Owner!
  requestId: Bytes!
  status: Int!
  randomNumber: Int
  winStatus: Int
}
