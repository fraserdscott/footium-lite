type Owner @entity {
  id: ID!
  formation: [Int!]!
}

type Player @entity {
  id: ID!
  owner: Owner!
  traits: [Int!]
  image: String!
}

type Match @entity {
  id: ID!
  accountA: Owner!
  accountB: Owner!
  requestId: Bytes!
  status: Int!
  randomNumber: Int
  winStatus: Int
}
